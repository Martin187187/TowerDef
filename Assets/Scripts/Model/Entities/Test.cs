using System.Collections.Generic;
using System.Linq;

public class Test : Effector
{

    /*
    public float value;
    
    public float CalculateValue()
    {
        float sum = value;
        foreach (var item in effects.OfType<TurrestStatsEffect>().Where((x) => x.modificationStat == TurrestStatsEffect.TurretStats.ATTACK_DAMGE))
        {
            sum+=item.getAdditionValue(this);
        }

        return sum;
    }
    */




}